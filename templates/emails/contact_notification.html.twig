<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
</head>
<body style="font-family: Arial, sans-serif; line-height: 1.6; color: #333;">
    <div style="max-width: 600px; margin: 0 auto; padding: 20px; border: 1px solid #ddd; border-radius: 5px;">
        <h2 style="color: #2c3e50; border-bottom: 2px solid #3498db; padding-bottom: 10px;">
            Nouveau message de contact
        </h2>

        <div style="margin: 20px 0;">
            <p><strong style="color: #2c3e50;">Nom :</strong> {{ contact.name }}</p>
            <p><strong style="color: #2c3e50;">Email :</strong> <a href="mailto:{{ contact.email }}">{{ contact.email }}</a></p>
            <p><strong style="color: #2c3e50;">Sujet :</strong> {{ contact.subject }}</p>
        </div>

        <div style="background-color: #f8f9fa; padding: 15px; border-left: 4px solid #3498db; margin: 20px 0;">
            <h3 style="color: #2c3e50; margin-top: 0;">Message :</h3>
            <p style="white-space: pre-wrap;">{{ contact.message|nl2br }}</p>
        </div>

        <div style="margin-top: 30px; padding-top: 20px; border-top: 1px solid #ddd; font-size: 12px; color: #7f8c8d;">
            <p>Ce message a été envoyé depuis le formulaire de contact de votre portfolio le {{ contact.createdAt|date('d/m/Y à H:i') }}</p>
        </div>
    </div>
</body>
</html>
